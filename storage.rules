rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Anyone can see the PDF (needed for public links)
      allow read: if true;
      
      // Only logged in users can upload files
      allow write: if request.auth != null;
    }
  }
}